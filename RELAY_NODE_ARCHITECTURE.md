# Relay Node MCT Validation Architecture

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    relay-node.html (UI Layer)                   │
└─────────────────────────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────┐
        │  Page loads in browser                    │
        │  BrowserRelayNode class instantiated      │
        │  OS auto-detected immediately             │
        └───────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────┐
        │  User clicks "🔗 Connect Wallet"         │
        └───────────────────────────────────────────┘
                              ↓
        ┌───────────────────────────────────────────────┐
        │  connectWallet() method executes:             │
        │  1. Requests wallet permission (MetaMask)     │
        │  2. Gets user's wallet address                │
        │  3. Displays shortened address in UI          │
        │  4. Shows wallet info section                 │
        │  5. Shows desktop node section                │
        └───────────────────────────────────────────────┘
                    ↙                    ↘
            ┌──────────────┐      ┌──────────────────┐
            │ async method │      │ sync method      │
            └──────────────┘      └──────────────────┘
                    ↓                     ↓
        ┌─────────────────────┐   ┌──────────────────────┐
        │checkMCTBalance()    │   │showOSDownloadOptions│
        ├─────────────────────┤   ├──────────────────────┤
        │ • Connect to        │   │ • Loop through 3 OS  │
        │   blockchain        │   │ • Compare with      │
        │ • Query MCT contract│   │   detected OS       │
        │ • Get balance       │   │ • Highlight YOUR OS │
        │ • Format decimals   │   │ • Add download      │
        │ • Update DOM        │   │   buttons           │
        │ • Call validate()   │   │ • Show instructions │
        └─────────────────────┘   └──────────────────────┘
                    ↓
        ┌────────────────────────────────────────┐
        │ validateTierRequirements()             │
        ├────────────────────────────────────────┤
        │ Get MCT_REQUIREMENTS[this.tier]        │
        │ Compare user's balance vs required     │
        │                                        │
        │ IF balance >= required:                │
        │   ✅ Show "Ready to register"         │
        │   ✅ Enable "Start Node" button        │
        │   ✅ Log success message               │
        │                                        │
        │ ELSE:                                  │
        │   ❌ Show "Need X more MCT"           │
        │   ❌ Disable "Start Node" button       │
        │   ❌ Log error message                 │
        └────────────────────────────────────────┘
                    ↓
        ┌────────────────────────────────────────┐
        │ User sees in browser:                  │
        ├────────────────────────────────────────┤
        │ ✅ Wallet: 0x1234...5678              │
        │ ✅ MCT Balance: 750.50 MCT             │
        │ ✅ OS Detected: 🍎 macOS               │
        │ ✅ Status: Ready to register           │
        │                                        │
        │ Desktop Node Downloads:                │
        │ • 🪟 Windows [Download]               │
        │ • 🍎 macOS [Download] ← HIGHLIGHTED   │
        │ • 🐧 Linux [Download]                 │
        └────────────────────────────────────────┘
                    ↓
        ┌────────────────────────────────────────┐
        │ User can now:                          │
        ├────────────────────────────────────────┤
        │ 1. Select different tier (re-validates)│
        │ 2. Start browser relay node            │
        │ 3. Download desktop relay node         │
        │ 4. Earn MCT tokens!                    │
        └────────────────────────────────────────┘
```

---

## Data Flow - MCT Balance Query

```
┌─────────────────────────────────────────────────────────────┐
│                         Browser                             │
│  User's Wallet: 0x1234567890abcdef                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
                  Uses ethers.js v6.7.1
                  BrowserProvider(window.ethereum)
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                   MetaMask Provider                         │
│  Handles RPC calls to blockchain                           │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                   Ramestta Network                          │
│  ChainID: 0x55A (1370 decimal)                             │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│             MCT Token Smart Contract                        │
│  Address: 0xEfD7B65676FCD4b6d242CbC067C2470df19df1dE     │
│                                                            │
│  Function: balanceOf(address account)                      │
│  Input: 0x1234567890abcdef                                │
│  Returns: 750500000000000000000 (wei)                     │
│                                                            │
│  Function: decimals()                                      │
│  Returns: 18                                               │
└─────────────────────────────────────────────────────────────┘
                              ↓
                  ethers.formatUnits()
              750500000000000000000 wei → 750.5 MCT
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    Browser DOM Update                       │
│  <span id="mctBalance">750.50 MCT</span>                   │
└─────────────────────────────────────────────────────────────┘
```

---

## OS Detection Flow

```
┌───────────────────────────────┐
│   detectOS() method called    │
└───────────────────────────────┘
         ↓
┌────────────────────────────────────────┐
│  Read navigator.userAgent              │
│  Example: "Mozilla/5.0 (Macintosh;    │
│  Intel Mac OS X 10_15_7)...Apple      │
└────────────────────────────────────────┘
         ↓
    ┌────────────────────┬────────────────────┬────────────────────┐
    │ Check for 'Win'   │ Check for 'Mac'   │ Check for 'Linux' │
    ├────────────────────┼────────────────────┼────────────────────┤
    │ Windows            │ macOS              │ Linux              │
    │ 🪟                 │ 🍎                 │ 🐧                 │
    │ supported: true    │ supported: true    │ supported: true    │
    └────────────────────┴────────────────────┴────────────────────┘
         ↓
┌───────────────────────────────────────────────────────┐
│  Return { name: 'macOS', icon: '🍎', supported: true}│
└───────────────────────────────────────────────────────┘
         ↓
    Stored in: this.osType
         ↓
┌──────────────────────────────────────────────────────────┐
│ showOSDownloadOptions() checks osType                   │
│ Highlights the matching OS with blue border             │
│ Adds "✅ For Your System" badge to user's OS           │
└──────────────────────────────────────────────────────────┘
```

---

## Tier Validation Logic

```
┌──────────────────────────────────────────┐
│  User selects tier (e.g., "Silver")     │
└──────────────────────────────────────────┘
         ↓
┌──────────────────────────────────────────────────────┐
│  validateTierRequirements() executes                 │
│  const requiredMCT = MCT_REQUIREMENTS['silver']      │
│  → 500                                               │
│                                                      │
│  const hasEnoughMCT = (750.5 >= 500)                │
│  → true                                              │
└──────────────────────────────────────────────────────┘
         ↓
         │
    ┌────┴────┐
    │          │
  true       false
    │          │
    ↓          ↓
 ┌──────────┐  ┌─────────────────────┐
 │ SUCCESS  │  │   INSUFFICIENT MCT  │
 ├──────────┤  ├─────────────────────┤
 │✅ Ready  │  │❌ Need X more MCT    │
 │to        │  │                     │
 │register  │  │Button: DISABLED     │
 │          │  │Color: Red/Gray      │
 │Button:   │  │Status: Can't start  │
 │ENABLED   │  │                     │
 │Color:    │  │Log: Error message   │
 │Green     │  │                     │
 │Status:   │  │Earn: 0 MCT until    │
 │Can start │  │      user gets MCT  │
 │Log:      │  │                     │
 │Success   │  │Show: "Need 250 more"│
 │message   │  │                     │
 └──────────┘  └─────────────────────┘
```

---

## State Management

```
┌────────────────────────────────┐
│  BrowserRelayNode Instance     │
├────────────────────────────────┤
│ Properties:                    │
│ ├─ wallet: "0x1234..."        │
│ ├─ mctBalance: 750.5          │
│ ├─ tier: "silver"             │
│ ├─ osType: { name, icon }     │
│ ├─ isRunning: false           │
│ ├─ earnings: 0.0000           │
│ ├─ messagesRelayed: 0         │
│ ├─ peers: Set(0)              │
│ └─ messageStore: Map(0)       │
├────────────────────────────────┤
│ Methods:                       │
│ ├─ connectWallet()            │
│ ├─ checkMCTBalance()          │
│ ├─ validateTierRequirements() │
│ ├─ showOSDownloadOptions()    │
│ ├─ detectOS()                 │
│ ├─ setTier()                  │
│ ├─ start()                    │
│ ├─ stop()                     │
│ └─ (+ more...)                │
└────────────────────────────────┘
         ↓
    Persists to
    localStorage
         ↓
    Survives page
    reloads
```

---

## UI Component Relationships

```
relay-node.html
│
├─── Wallet Connection Section
│    ├─ <button> "Connect Wallet"  → onclick="relayNode.connectWallet()"
│    └─ <div id="walletInfo">      → hidden until connected
│        ├─ walletAddressDisplay   ← Updates after connection
│        ├─ mctBalance             ← Updates after MCT check
│        ├─ osType                 ← Updates after OS detection
│        └─ mctStatus              ← Updates after validation
│
├─── Node Dashboard Section       → hidden until connected
│    ├─ Status Cards
│    ├─ Tier Selector
│    │  ├─ Bronze     (100 MCT)
│    │  ├─ Silver     (500 MCT)
│    │  ├─ Gold       (1000 MCT)
│    │  └─ Platinum   (2500 MCT)
│    ├─ Node Controls
│    └─ Console Output
│
├─── Desktop Node Section         → hidden until connected
│    ├─ Windows Download Card     ← Highlighted if user is on Windows
│    ├─ macOS Download Card       ← Highlighted if user is on macOS
│    └─ Linux Download Card       ← Highlighted if user is on Linux
│
└─── Documentation Section
     ├─ What is a Relay Node?
     ├─ Requirements
     ├─ How it Works
     ├─ Reward Tiers
     └─ Tips & Tricks
```

---

## Code Execution Timeline

```
Time  Event                              Method Called      DOM Updated
────  ─────────────────────────────────  ─────────────────  ──────────────
  0   Page loads                         constructor()      osType detected
  +   User clicks button                 -                  -
  1   Connect MetaMask                   connectWallet()    -
  2   Wallet approved                    -                  -
  3   Get wallet address                 -                  walletAddress ✓
  4   Show wallet info                   -                  walletInfo ✓
  5   Show desktop section               -                  desktopNode ✓
  6   Query MCT balance                  checkMCTBalance()  mctBalance ✓
  7   Validate tier requirements         validateTierReq()  mctStatus ✓
  8   Show OS-specific downloads         showOSDownloads()  osDownloadSection ✓
  9   User ready to start node           -                  startBtn enabled ✓
```

---

## Error Handling

```
┌────────────────────────────────────────┐
│  connectWallet() called                │
└────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│  Check: window.ethereum exists?                       │
├────────────────────────────────────────────────────────┤
│ YES ✅                          │  NO ❌               │
│ Continue                        │  Show error:         │
│                                 │  "MetaMask not found"│
│                                 │  Return false        │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│  Request account access                               │
├────────────────────────────────────────────────────────┤
│ SUCCESS ✅                  │  ERROR ❌              │
│ Get wallet address          │  Show error message    │
│ Continue                    │  Return false          │
└────────────────────────────────────────────────────────┘
         ↓
┌────────────────────────────────────────────────────────┐
│  checkMCTBalance() called                             │
├────────────────────────────────────────────────────────┤
│ BLOCKCHAIN CONNECTION EXISTS ✅   │  NO CONNECTION ❌ │
│ Query contract                    │  Show warning      │
│ Get balance                       │  Offline mode      │
│ Validate tier                     │  Continue safely   │
└────────────────────────────────────────────────────────┘
```

---

## Security Considerations

```
Read-Only Operations (Safe ✅)
├─ balanceOf(address) - Blockchain read
├─ decimals() - Contract metadata
├─ navigator.userAgent - Local OS info
└─ localStorage - Local storage only

No Write Operations (No Risk)
├─ No token transfers
├─ No smart contract state changes
├─ No private key access
└─ No transaction signing needed

User Control (Always Explicit)
├─ MetaMask approval required for wallet
├─ User chooses tier manually
├─ User initiates node start/stop
└─ Downloads are external links only
```

---

**This architecture ensures smooth, automatic MCT validation and OS detection while maintaining security and user control.**
